# Production build config, embedded into the Pages bundle by `vite build`.
# BOTH values are PUBLIC (the Google client id is exposed in-browser by design, and
# the Worker URL is called from the browser) — safe to commit. Edit here to change
# them; no GitHub-settings step needed. Local dev overrides these via .env.local.
VITE_API_URL=https://zombiefarm-server.zombiefarm.workers.dev
VITE_GOOGLE_CLIENT_ID=249211577622-cmqa6so9re0hgh2lti4mpe40nlcm3l4k.apps.googleusercontent.com
